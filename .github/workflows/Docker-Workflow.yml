on:
  push 

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say-Hello
        run: echo "Hello world!"
      - name: Install-packages
        run: sudo apt install fortune
      - name: get-fortune
        run: fortune
